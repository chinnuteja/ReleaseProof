# Local durable runtime
DATABASE_PATH=var/releaseproof.sqlite
APP_ORIGIN=http://127.0.0.1:3000
OPERATOR_PASSWORD=
SESSION_SECRET=
RELEASEPROOF_MODE=fixture
RELEASEPROOF_ENVIRONMENT_PATH=config/fixture-environment.json

# Application model (required only for real model smoke checks and planning)
OPENAI_API_KEY=
OPENAI_MODEL=

# Dedicated test resources only; never paste production values into this file
GITHUB_TOKEN=
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
LINEAR_API_KEY=
