# P2 — Authorized three-app release (fixture)

Status: **LOCAL_VERIFIED** for the fixture vertical slice. The real three-app gate remains **BLOCKED**.

Tested implementation digest: `94126386251a1ae3d9cf1518b003bd94b088fc46128d85581652cfaa5dd8db92`

## What was implemented

Bounded read-only planner; server-side immutable manifest hashing; Slack approval validation (team/channel/message/action/reviewer/nonce/expiry, transport dedupe); restricted executor that publishes only the approved SHA through provider adapters; Linear state + receipt comment; Slack final readback; run/events/command/receipt APIs; operator product workspace driven by persisted state.

Fixture HTTP services implement GitHub/Linear/Slack request shapes. Production adapters send HTTP to those services. Fixture approval injection uses the same `ingestApproval` validator as Socket Mode would. There is no browser approval route.

## Commands and observed results

| Command | Exit | Observed result |
| --- | ---: | --- |
| `npm run check` | 0 | Typecheck, lint, and production build passed. |
| `npm run verify -- --phase P2 --mode fixture` | 0 | 15 tests passed across `tests/domain/authority.test.ts` and `tests/integration/p2-scenarios.test.ts`. |
| `npm run verify -- --phase P2 --mode real_test --env demo --allow-test-writes` | 2 | Real P2 gate refused a pass. No GitHub/Linear/Slack writes were attempted. |

Provider modes for the passing suite: `{ github: fixture, slack: fixture, linear: fixture }`. Model used: deterministic evidence-following fixture model, not OpenAI.

## Scenario outcomes

| ID | testStatus | actualBusinessOutcome |
| --- | --- | --- |
| SC-01 | PASS | Completed prerelease at SHA A; Linear released state + receipt comment observed; Slack final receipt observed. |
| SC-02 | PASS | After branch/PR head moved to B, executor still published approved A; no B release. |
| SC-03 | PASS | Replan to B left A's approval unable to publish B; `publishAttempts` remained 0. |
| SC-04 | PASS | Annotated tag at B blocked publish of A; no release created. |
| SC-06 | PASS | Wrong reviewer/team/channel/message/nonce/expiry rejected; duplicate transport delivery did not create another protected attempt; valid approval then completed once. |
| SC-07 | PASS | Wrong SHA, wrong producer, pending, and failed checks produced `needs_attention` and zero publishes. |
| SC-11 | PASS | Second authority for the same environment/repository/tag was rejected; existing A release retained. |
| P2-G6 | PASS | Malformed/refused planner output and malicious issue text produced no provider mutation. |
| Authority unit checks | PASS | Manifest hash changes with release-critical fields; invalid Slack identities rejected; no `/api/approvals` route exists. |

## Real gate

P2-G1 (actual model, real GitHub/Linear writes, genuine allowlisted Slack approval) is **BLOCKED**. Required missing configuration: `OPENAI_API_KEY`, `OPENAI_MODEL`, `GITHUB_TOKEN` scoped to `chinnuteja/ReleaseProof-demo`, Slack app/channel/reviewer IDs, Linear team/issue/state. No demo release/tag was created in `chinnuteja/ReleaseProof`.

## Next action

After credentials are present, run only the authorized real-test command against `chinnuteja/ReleaseProof-demo` with `--allow-test-writes`. Fixture success is not real success.
