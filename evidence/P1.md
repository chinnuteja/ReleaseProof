# P1 — Foundation, contracts, and provider access

Status: **IN_PROGRESS** — local runtime foundation is implemented and its focused fixture checks pass. The P1 external provider and model gates remain blocked because no dedicated provider resources or application credentials have been configured.

Tested implementation digest: `12ef243b84769c38364aa1a8f30eb32f31bd5e6d6bb6ef4d81eb957d09ce7082`

## Environment and dependency record

- Runtime selected: project-local Node `v24.21.0` and npm `11.19.0` on Windows.
- Host Node `v22.14.0` was not used for verification because it is EOL; ADR-08 records the Node 24 correction.
- Pinned dependencies: Next `16.3.5`, React `19.3.0`, TypeScript `6.0.3`, Zod `4.6.4`, better-sqlite3 `13.0.3`, OpenAI `7.15.0`, Slack Socket Mode `3.0.1`, and Slack Web API `8.1.1`.
- Provider modes exercised: GitHub/Slack/Linear fixture only.

## Commands and observed results

| Command | Exit | Observed result |
| --- | ---: | --- |
| `npm run typecheck` with project-local Node 24 on `PATH` | 0 | Strict workspace compilation passed. |
| `npm run lint` with project-local Node 24 on `PATH` | 0 | ESLint and import-boundary rules passed. |
| `npm run db:migrate` with project-local Node 24 on `PATH` | 0 | Applied `001_initial.sql` to `var/releaseproof.sqlite`. |
| `npm run doctor -- --mode fixture` with project-local Node 24 on `PATH` | 0 | Node 24 confirmed; SQLite writable; fixture URLs limited to loopback. |
| `npm run verify -- --phase P1 --mode fixture` with project-local Node 24 on `PATH` | 0 | Five focused foundation assertions passed. |
| `npm run check` with project-local Node 24 on `PATH` | 0 | Typecheck, lint, and production Next.js build passed. |
| `npm run doctor -- --mode real_test --env demo` with project-local Node 24 on `PATH` | 1 | Correctly blocked: GitHub, Slack, Linear, OpenAI key/model configuration absent. |
| `npm ci` with project-local Node 24 on `PATH` | NOT_COMPLETED | First attempt was blocked by a project Next.js process holding its native binary. After stopping that process, a retry produced no output and was manually terminated after bounded waits. `npm install` then restored the expected executable shims and passed. This clean-install gate is still unverified. |

## Scenario and inspection evidence

- `tests/domain/foundation.test.ts`: 5 passing assertions covering request-key idempotency/conflict, loopback fixture enforcement, GraphQL/unsupported normalization, redaction, worker singleton/restart processing, and migration checksum mismatch.
- Local two-process inspection: worker bound `127.0.0.1:4319`; `GET /api/health` returned `200` with a persisted worker heartbeat; `GET /api/connections` returned the three redacted, unconfigured provider states.
- No external provider write, model call, Slack message, or release was performed.

## Gate assessment

- P1-G2, P1-G3, and P1-G4: covered locally by the focused fixture checks and inspection.
- P1-G1: **NEEDS_FIX** until a clean `npm ci` and build complete without the Windows file-lock/hang behavior.
- P1-G5: **BLOCKED** — dedicated GitHub repository, Slack workspace/channel/app, and Linear team are not configured.
- P1-G6: **BLOCKED** — `OPENAI_API_KEY` and `OPENAI_MODEL` are not configured; no fake model response was used.

## Next action

Diagnose and complete the clean-install behavior, then implement real provider identity/capability probes and record their actual results when the dedicated credentials and resources are available.

## Source-control handoff

- Local foundation commit: `f2c8ed1` (`chore: establish releaseproof runtime and provider contracts`).
- Handoff-state commit: `05be064` (`docs: record phase one handoff blocker`).
- Remote: `origin` is `https://github.com/chinnuteja/ReleaseProof.git` on `main`.
- Push result: **SYNCED**. After GitHub device authentication as `chinnuteja`, `origin/main` was verified to resolve to `05be064d427edb6d9519f817228cd6fc07a4348b`, matching local `HEAD` at the time of verification.
