# P1 — Foundation, contracts, and provider access

Status: **LOCAL_VERIFIED** for fixture/runtime gates. External provider and model gates remain **BLOCKED** because `.env` has no real credentials.

Tested implementation digest: `94126386251a1ae3d9cf1518b003bd94b088fc46128d85581652cfaa5dd8db92`

## Environment and dependency record

- Runtime selected: project-local Node `v24.21.0` and npm `11.19.0` on Windows (`PATH` prefixed with `.tools/node-v24.21.0-win-x64`).
- Host Node `v22.14.0` was not used for verification (ADR-08).
- Pinned dependencies: Next `16.3.5`, React `19.3.0`, TypeScript `6.0.3`, Zod `4.6.4`, better-sqlite3 `13.0.3`, OpenAI `7.15.0`, Slack Socket Mode `3.0.1`, Slack Web API `8.1.1`.
- Provider modes exercised: GitHub/Slack/Linear fixture only.

## npm ci reproduction

The previous session left a broken `node_modules` (no `.bin` shims, no `.package-lock.json`) after an interrupted `npm ci`. That tree was leftover install state, not a lockfile defect.

Reproduction this session:

| Location | Command | Exit | Observed result |
| --- | --- | ---: | --- |
| Disposable clone `C:\tmp\rp-npm-ci-probe` (no existing `node_modules`) | `npm ci --no-fund --no-audit` with Node 24.21.0 | 0 | 176 packages; `.bin` shims for `tsx`/`tsc`/`vitest`/`next`/`eslint`; `better-sqlite3` loaded from `prebuilds/win32-x64.node` |
| Actual repository root | `npm ci --no-fund --no-audit` with Node 24.21.0 | 0 | Same complete install after replacing the interrupted tree |

Repository hardening applied: `.npmrc` with `engine-strict=true` and `bin-links=true`; doctor now fails if `.bin` shims or `better-sqlite3` cannot load. The official gate remains `npm ci`, not `npm install`.

A leftover previous-agent checkout exists at gitignored `artifacts/private/clean-checkout-p1` and was not used for implementation.

## Commands and observed results

| Command | Exit | Observed result |
| --- | ---: | --- |
| `npm ci` with project-local Node 24 on `PATH` | 0 | Clean lockfile install restored shims and SQLite prebuild. |
| `npm run db:migrate` | 0 | Applied `002_p2_authority.sql` to `var/releaseproof.sqlite`. |
| `npm run typecheck` | 0 | Strict workspace compilation passed. |
| `npm run lint` | 0 | ESLint and import-boundary rules passed (`artifacts/**` ignored). |
| `npm run check` | 0 | Typecheck, lint, and production Next.js build passed. |
| `npm run doctor -- --mode fixture` | 0 | Node 24 confirmed; SQLite binding loaded; fixture URLs loopback-only. |
| `npm run verify -- --phase P1 --mode fixture` | 0 | Five foundation assertions passed. |
| `npm run doctor -- --mode real_test --env demo` | 2 | Correctly blocked: GitHub, Slack, Linear, OpenAI key/model absent. |
| `npm run model:smoke` / `tsx scripts/model-smoke.ts` | 2 | Correctly blocked: `OPENAI_API_KEY` and `OPENAI_MODEL` absent. No fake model result. |
| `npm run verify -- --phase P1 --mode real_test` | 2 | Real P1 registry refused a pass without credentials and probes. |

## Scenario and inspection evidence

- `tests/domain/foundation.test.ts`: 5 passing assertions covering request-key idempotency/conflict, loopback fixture enforcement, GraphQL/unsupported normalization, redaction, worker singleton/restart processing, and migration checksum mismatch.
- CI workflow added at `.github/workflows/ci.yml` for `npm ci`, migrate, check, fixture doctor, and P1/P2 fixture verify. This commit's CI status is not yet observed.
- Real provider capability probes and the structured-output smoke path exist and exit 2 when credentials are missing. They were not executed against live providers.

## Gate assessment

- P1-G1: **LOCAL_VERIFIED** — clean `npm ci` and production build completed on Node 24.21.0.
- P1-G2, P1-G3, P1-G4: covered locally by the focused fixture checks.
- P1-G5: **BLOCKED** — dedicated GitHub repository, Slack workspace/channel/app, and Linear team are not configured.
- P1-G6: **BLOCKED** — `OPENAI_API_KEY` and `OPENAI_MODEL` are not configured; no fake model response was used.

## Next action

When credentials exist, run `npm run doctor -- --mode real_test --env demo` and `npm run model:smoke` against the dedicated demo resources. Do not treat fixture results as that gate.
