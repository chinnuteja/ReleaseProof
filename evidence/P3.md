# P3 — Combined recovery, proof, and product (local)

Status: **LOCAL_VERIFIED** for the implemented fixture recovery and workspace slice. Real-provider verification remains **BLOCKED** until the supplied credentials and a genuine Slack approval are available.

## Current local proof

| Command | Exit | Result |
|---|---:|---|
| `npm run typecheck` | 0 | Contracts, provider adapters, workflow recovery, and web UI compile. |
| `npm run verify -- --phase P3 --mode fixture` | 0 | 15 scenarios passed. |
| `npm run build --workspace @releaseproof/web` | 0 | Production web build passed. |
| `npm run eval` | 0 | Deterministic fixture evaluation passed. |

The fixture suite includes lost-response recovery for GitHub publication, Linear receipt creation, Slack final-message update, and Slack approval-message posting. The recovery invariant is read-before-retry: once a write was sent, ReleaseProof reconciles an immutable marker and does not issue a blind duplicate write.

## Implemented product surface

- release command center with connection state, five-stage flow, immutable authority, provider receipt, event trail, and safe recovery controls;
- deterministic `eval`, `replay`, and `export:evidence` commands;
- annotated Git-tag resolution and fail-closed malformed Linear response handling.

## Still external / final-delivery work

- real GitHub, Linear, Slack, and model verification using the dedicated demo environment;
- genuine allowlisted Slack approval and real readback proof;
- final deployment, README, and commit-to-remote verification in P6.
